node_modules
.next
out
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Local design bundle artifacts
design-bundle.tar
design-bundle.gz
design-extract/
.vercel

# Claude Code (local tooling — skills, settings, etc.)
.claude/

# Local one-off scripts (kept locally, not committed)
scripts/build-tech-content-doc.py
scripts/refactor/
